#include <iostream>
using namespace std; 

int main(){
    freopen("balancing.in", "r", stdin);
    freopen("balancing.out", "w", stdout);
    int n, b; cin >> n >> b;
    int ans = n; 
    while(n--){
        long long a,c = 0;
        cin >> a >> c;
    }
    ans+=3;
    cout << ans/4 ;
}